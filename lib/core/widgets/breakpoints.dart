/// Layout breakpoints for the adaptive shell.
class NoorBreakpoints {
  /// At or above this width the navigation rail is used (TV/desktop/tablet);
  /// below it the bottom navigation bar is used (phone).
  static const double rail = 700;
}
