/// How a playlist's content is sourced.
enum PlaylistType { xtream, m3u, upload }

/// What kind of media an item is — used to key favorites and watch progress.
enum MediaKind { channel, movie, episode }
